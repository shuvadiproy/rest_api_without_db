{
  "name": "rest_api_without_db",
  "version": "1.0.0",
  "description": "It is a simple REST API without any database. Following MVC architecture. routes   - /users -> GET -> {id, username, email}   - /users/ -> POST -> {username, email}   - /users/:id -> PUT -> update the user   - /users/:id -> DELETE -> delete the user",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shuvadip Roy",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "nodemon": "^3.0.2",
    "uuid": "^9.0.1"
  }
}
